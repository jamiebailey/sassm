import React from 'react';
import Save from './Save';

export default class SaveList extends React.Component {
    render() {
        return (
            <div>
                <Save />
                <Save />
                <Save />
            </div>
        )
    }
}