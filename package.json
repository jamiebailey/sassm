{
  "name": "example",
  "version": "1.0.0",
  "main": "dist/app.js",
  "bin": "dist/app.js",
  "author": "Jamie Bailey <jamie.bailey.dev@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "babel ./ --out-dir dist/ --ignore ./node_modules,./dist,./bin./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/app.js",
    "export": "npm run build && pkg --output=bin/app ."
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "express": "^4.17.1",
    "find": "^0.3.0",
    "pkg": "^4.4.2"
  }
}
